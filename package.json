{
  "name": "pague-lo-suyo",
  "version": "1.0.0",
  "private": true,
  "description": "App para dividir la cuenta entre un grupo de personas",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "dependencies": {
    "@upstash/redis": "^1.34.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "http-server": "^14.1.1"
  }
}
